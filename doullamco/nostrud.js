var analysisTimeout = 0;

// Later in your code, you might update the value of analysisTimeout
analysisTimeout = 5000; // Now analysisTimeout is 5000

// You can also use it in conditions or calculations
if (analysisTimeout > 0) {
    console.log("Timeout is set to", analysisTimeout, "milliseconds.");
} else {
    console.log("Timeout is not set.");
}
